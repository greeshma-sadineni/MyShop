@model IEnumerable<MyShop.core.Models.Product>

@{
    ViewBag.Title = "Home Page";
}
<h1>Products</h1>
@*<form asp-controller="Home" asp-action="Index" method="get">
        <p>
            Category: <input type="text" name="SearchString">
            <input type="submit" value="Filter" />
        </p>
    </form>
    @foreach (var item in Model)
    {
        <li>
            @Html.ActionLink(item.Category, "FilterProduct", "Home", new { Category = item.Category }, null)|

        </li>}*@
<div class="col-md-2">
    <h4>Filter By Category</h4>
    <div class="list-group"></div>
    @Html.ActionLink("All", "Index", null, new { })

</div>

@foreach (var item in Model)
{
    <div class="col-md-4" style="height:450px;border :solid thin whitesmoke">
        <div class="col-md-12">
            <img class="img" style="height:250px;" src="~/Content/ProductImages/@item.Image" />
        </div>
        <div class="col-md-12">
            <strong>@Html.ActionLink(item.Name, "Details", new { id = item.Id })</strong>
        </div>
        <div class="col-md-12">
            <p>@item.Description</p>
        </div>
        <div class="col-md-12">
            <h4>@item.Price</h4>
        </div>
        <div class="col-md-12">
            <a href="#" class="btn btn-default"> Add to Basket</a>

        </div>
    </div>
}
<div class="col-md-12 clearfix">



    @*@model IEnumerable<MyShop.core.Models.Product>
        @{
            ViewBag.Title = "Home Page";
        }
        <h1>Products</h1>
        @foreach (var item in Model)
        {
            <li>
        @item.Category

            </li>
        }

        @foreach (var item in Model)
        {
            <div class="col-md-4" style="height:450px;margin:10px ;border :solid thin whitesmoke">
                <div class="col-md-12">
                    <img class="img" style="height:250px;" src="~/Content/ProductImages/@item.Image" />
                </div>
                <div class="col-md-12">
                    <strong>@Html.ActionLink(item.Name, "Details", new { id = item.Id })</strong>
                </div>
                <div class="col-md-12">
                    <p>@item.Description</p>
                </div>
                <div class="col-md-12">
                    <h4>@item.Price</h4>
                </div>
                <div class="col-md-12">
                    <a href="#" class="btn btn-default"> Add to Basket</a>

                </div>
            </div>
        }
        <div class="col-md-12 clearfix">*@
